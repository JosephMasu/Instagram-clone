server:
  port: 8084

spring:
  application:
    name: notification-service

  mongodb:
    uri: mongodb://localhost:27017/instagram_notifications_test

  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false

jwt:
  secret: test-secret-that-is-long-enough-for-hmac-sha-256
