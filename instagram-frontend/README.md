# Frontend

Angular 22 app for this clone. Setup, ports, and architecture are in the **[root README](../README.md)**.

```bash
npm install
npm start
```

Opens http://localhost:4200 and proxies `/api` to the gateway on port 8080.
